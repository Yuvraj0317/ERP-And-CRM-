version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: mini_operations_erp_db
    environment:
      POSTGRES_USER: erp_user
      POSTGRES_PASSWORD: erp_password
      POSTGRES_DB: mini_operations_erp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U erp_user -d mini_operations_erp"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
